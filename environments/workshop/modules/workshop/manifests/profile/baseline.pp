# Baseline Profile
# Common configuration for all nodes
# Wraps the workshop::baseline class

class workshop::profile::baseline {
  # Include the baseline configuration class
  include workshop::baseline
}
